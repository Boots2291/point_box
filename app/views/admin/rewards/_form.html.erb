<%= form_for([:admin, @reward]) do |f| %>
  <div class="form_group">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="form_group">
    <%= f.label :cost %>
    <%= f.number_field :cost %>
  </div>
  <%= f.submit class: 'btn btn-success' %>
<% end %>